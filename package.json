{
  "name": "substrate",
  "version": "0.0.0",
  "private": true,
  "description": "Research agenda: five benchmarkable units around one conviction — agents fail when a wrong decision ships undetected.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
