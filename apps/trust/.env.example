# @substrate/trust (02) local env template — cp .env.example .env, then fill in.
# SUBSTRATE_PEAK_* vars are NEVER a runtime dependency (peak validation only).
POSTGRES_USER=substrate
POSTGRES_PASSWORD=change-me
POSTGRES_DB=substrate
MINIO_ROOT_USER=substrate
MINIO_ROOT_PASSWORD=change-me
TRUST_PG_DSN=postgresql://substrate:change-me@localhost:5432/substrate
TRUST_MINIO_ENDPOINT=localhost:9000
OLLAMA_BASE_URL=http://localhost:11434
LLAMA_SERVE_URL=http://localhost:8080
