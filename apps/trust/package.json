{
  "name": "@substrate/trust",
  "version": "0.0.0",
  "private": true,
  "description": "One measurement discipline for actions and data — confidence from evidence, gated synthetic data.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@substrate/substrate": "workspace:*",
    "@substrate/scenarios": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}