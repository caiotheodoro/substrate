{
  "name": "@substrate/knowledge",
  "version": "0.0.0",
  "private": true,
  "description": "Retrieval operated like a database — extraction evals, entity resolution, freshness, grounded gates.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@substrate/substrate": "workspace:*",
    "@substrate/scenarios": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}