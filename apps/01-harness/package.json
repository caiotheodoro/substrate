{
  "name": "@substrate/harness",
  "version": "0.0.0",
  "private": true,
  "description": "The deterministic, gated, replayable agent runtime — the center of the ecosystem.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@substrate/substrate": "workspace:*",
    "@substrate/scenarios": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}