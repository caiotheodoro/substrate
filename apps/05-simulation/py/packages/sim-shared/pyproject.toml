[project]
name = "substrate-sim-shared"
version = "0.0.0"
description = "Cross-cutting primitives shared by all 05 molecules: C1 canonical JSON/chain hash, seeded RNG registry, OpenAI-compatible LLM client, cost meter."
requires-python = ">=3.11"
dependencies = ["numpy>=1.26", "httpx>=0.27"]

[tool.pytest.ini_options]
testpaths = ["tests"]