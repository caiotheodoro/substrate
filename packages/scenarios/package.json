{
  "name": "@substrate/scenarios",
  "version": "0.0.0",
  "description": "Historical shocks and synthetic worlds — the shared scenario library.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}